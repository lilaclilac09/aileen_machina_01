# Bundled Luma guest list for Cafe Cursor Shanghai
#
# `luma-guests.csv` — Luma Guests export (approved rows auto-imported on deploy).
# Do not commit newer exports with extra PII unless needed for a re-sync.
