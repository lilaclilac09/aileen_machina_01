# Task ladder

Date: 2026-09-25.

This file is the task bar. It is not a second brain. Skills still promote only through `pnpm evolve` when held-out improves. Dreaming still writes `proposals/` only. Visitor soft memory stays in Redis. No weight update. No VM is claimed on the public console until a later rung has command evidence.

Next rung starts only after the previous one has command output. A failed attempt needs a lesson in `ops/lessons/` before a retry. Do not mark a rung from reading the code.

## Rungs

| rung | axis | task | status | evidence |
| --- | --- | --- | --- | --- |
| R0 | tool-use | Classify only: `taste` / `latest_updates` / `hire_cv` / `soft_recall` / `evolvement` | passed | `cargo test --test lanes` 2026-09-25, 2 passed. `evolvement-rs/tests/lanes.rs` |
| R1 | honesty under Chinese query | `更新了什么吗` cites `latest-content.md` by date | not started | |
| R2 | isolation | "what did I ask" never returns the setlist | not started | |
| R3 | isolation | VM smoke: echo + rg in `/work`, stop, no host leak | blocked | no computer. Docker/Firecracker not used. Do not tell a visitor there is a machine. |
| R4 | tool-use | Harvest watchlist inside the VM, digest only via promote | not started | waits on R3 |
| R5 | coverage | Skill change only if `pnpm verify:evolve` held-out goes up | not started | |
| R6 | honesty under Chinese query | Evolvement card: dream date + harvest date + this visitor's topics | not started | |
| R7 | isolation | Missing `REDIS_URL` degrades. Missing `GITHUB_TOKEN` harvest is read-only | not started | |

R0 does not include repo harvest, a computer, or a public pitch. Those are later rungs.

## Rule

Refuse R(n+1) until R(n) has command output, a fixture or a production URL, and a lesson file if that rung failed once.
