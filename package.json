{
  "name": "aileen-machina-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:chat": "cd chat && npm install",
    "dev:chat": "cd chat && npm run dev",
    "build:chat": "cd chat && npm run build",
    "deploy": "netlify deploy --prod"
  },
  "devDependencies": {
    "@hono/cli": "^0.1.10",
    "@types/dotenv": "^6.1.1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.99.0",
    "dotenv": "^17.3.1"
  }
}
