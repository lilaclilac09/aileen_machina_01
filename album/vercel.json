{
  "buildCommand": "node scripts/ensure-prisma-provider.mjs && prisma generate && prisma db push && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1", "hnd1"],
  "crons": [
    {
      "path": "/api/cron/purge",
      "schedule": "0 4 * * *"
    }
  ]
}
